#!/usr/bin/env ruby

require_relative "../lib/play"

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
play(board)
input = gets.strip
index = input_to_index(input)
move(board, index, "X")# Make sure to execute #play below
